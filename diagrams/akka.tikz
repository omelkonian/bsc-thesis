\begin{tikzpicture}
  [every matrix/.append style={column sep=.5cm}]
  \matrix{  
    \node[dataflow] (A) {\small in};
    \& \node[dataflow] (B) {\small bcast};  
    \& \node[dataflow] (C) {\small merge};
    \& \node[dataflow] (D) {\small out}; \\
  };
  
  \draw[to] (A) -- node[midway] {f1} (B);
  \draw[to] (B) to[bend left] node[midway] {f2} (C);
  \draw[to] (B) to[bend right] node[midway] {f4} (C);
  \draw[to] (C) -- node[midway] {f3} (D);
\end{tikzpicture} 