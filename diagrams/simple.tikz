\begin{tikzpicture}[
  every matrix/.style={ampersand replacement=\&,column sep=1cm,row sep=0.5cm},
  every node/.style={align=center}]

  \matrix{
    \node (fst) {\tiny{1..10}}; \\
    \& \node (zip) {\tiny{zip(+)}};  
    \& \node (out) {\tiny{2,4,6,8...}}; \\
    \node (snd) {{\tiny 1..10}}; \\
  };
  \draw (fst) -- (zip);
  \draw (snd) -- (zip);
  \draw (zip) -- (out);  
\end{tikzpicture} 